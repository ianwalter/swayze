name: CI
on: pull_request
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: node:12.21-slim
    steps:
      - name: Checkout
        uses: actions/checkout@master
      - name: Install
        run: yarn
      - name: Test
        run: yarn test
